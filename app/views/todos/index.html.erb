<h2>
<%= link_to 'Add New', new_todo_path %>
Todos
</h2>
<ul>
<% @todos.each do |todo| %>
 <li>
　<%= link_to todo.body,todo_path(todo) %>
  <%= render 'likes/show', todo: todo %>
  <%= link_to 'delete', 
  todo_path(todo), method: :delete, 
  data: { confirm: 'Sure?' } %>
 </li>
<% end %>
</ul>
<a href="/auth/twitter"><button>ログイン</button></a>
